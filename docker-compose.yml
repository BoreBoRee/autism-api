version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3001:5000
    # environment:
    #   - UPLOAD_PATH=/images
    #   - UPLOADED_PATH=/images
    # volumes:
    #   - ./images:/images
